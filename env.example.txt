# Rename this file to ".env" after downloading (remove the "example" part).
# Then fill in real values below. Do not commit ".env" anywhere public.

# --- Detection tuning (currently set for a 2-minute lookback window) ---
# Note: Polymarket US's public market-data API doesn't expose volume yet,
# so detection is price-move only (no volume-spike alert / liquidity filter).
POLL_INTERVAL_SECONDS=15
LOOKBACK_MINUTES=2
PRICE_MOVE_THRESHOLD=0.035
COOLDOWN_MINUTES=30

# --- Which sports to track ---
# Comma-separated. Options: ufc, nfl, soccer, mlb, cfb
# (Polymarket US has no "sport" field, only per-team league codes, so "soccer"
# means every domestic/international soccer league - see market_sport() in
# polymarket_alert_bot.py if you need to add/exclude a specific league.)
SPORTS_FILTER=ufc,nfl,soccer,mlb,cfb

# --- Local dashboard (top movers UI) ---
DASHBOARD_HOST=127.0.0.1
DASHBOARD_PORT=8787
DASHBOARD_TOP_N=150

# --- ntfy.sh push notifications ---
# No account needed - pick a hard-to-guess topic name (it's public: anyone
# who knows/guesses it can read your alerts) and subscribe to it in the
# ntfy app or at https://ntfy.sh/<topic> in a browser. See README.md.
NTFY_SERVER=https://ntfy.sh
NTFY_TOPIC=your-unique-topic-name-here
NTFY_PRIORITY=default
